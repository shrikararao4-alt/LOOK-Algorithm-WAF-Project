{
  "name": "look-disk-scheduling-mern",
  "version": "1.0.0",
  "description": "MERN Stack LOOK Disk Scheduling Visualizer",
  "scripts": {
    "install-backend": "npm install --prefix backend",
    "install-client": "npm install --prefix look-project",
    "install-all": "npm run install-backend && npm run install-client && npm install",
    "server": "npm start --prefix backend",
    "client": "npm start --prefix look-project",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
